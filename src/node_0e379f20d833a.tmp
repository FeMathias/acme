var bucket = [];

for (var i=0;i<=50;i++) {
    bucket.push(i);
}

function getRandomFromBucket() {
   var randomIndex = Math.floor(Math.random()*bucket.length);
   return bucket.splice(randomIndex, 1)[0];
}

let Data = [{}]

for (var object=0; 1<=12; object++) {
  let object = {
    id: 21,
    nomeCompleto: '',
    value: '',
    isFavorite: false
  }
  Data.push(object)
}

console.log(Data)